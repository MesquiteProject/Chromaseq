/* Mesquite Chromaseq source code.  Copyright 2005-2010 David Maddison and Wayne Maddison.Version 0.980   July 2010Disclaimer:  The Mesquite source code is lengthy and we are few.  There are no doubt inefficiencies and goofs in this code. The commenting leaves much to be desired. Please approach this source code with the spirit of helping out.Perhaps with your help we can be more than a few, and make Mesquite better.Mesquite is distributed in the hope that it will be useful, but WITHOUT ANY WARRANTY.Mesquite's web site is http://mesquiteproject.orgThis source code and its compiled class files are free and modifiable under the terms of GNU Lesser General Public License.  (http://www.gnu.org/copyleft/lesser.html) */package mesquite.chromaseq.OpenCloseGaps; import mesquite.lib.*;import mesquite.lib.characters.CharacterData;import mesquite.meristic.lib.MeristicData;import mesquite.categ.lib.*;import mesquite.chromaseq.ViewChromatograms.ChromaseqUniversalMapper;import mesquite.chromaseq.lib.*;/* ======================================================================== */public class OpenCloseGaps extends ChromInit {	ChromatWindow window;	/*.................................................................................................................*/	public boolean startJob(String arguments, Object condition, boolean hiredByName) {		return true;	}	public void setWindow(MesquiteWindow w){		ChromatogramTool 	openGapTool = new ChromatogramTool(this, "insertBase", getPath(), "addChars.gif", 7, 15,"Insert Base", "This tool inserts a base in the edited sequence.", MesquiteModule.makeCommand("insertBase",  this) , null, null);		openGapTool.setWorksOnAllPanels(false);		openGapTool.setWorksOnEditableSequencePanel(true);		w.addTool(openGapTool);		openGapTool.setIsInBetween(true);		if (w instanceof ChromatWindow)			window = (ChromatWindow)w;	}	/*.................................................................................................................*/	public Object doCommand(String commandName, String arguments, CommandChecker checker) {		if (checker.compare(this.getClass(),  "Inserts a base", "[site touched][local site][is chromatogram panel, true or false][which panel touched]", commandName, "insertBase")) {			if (table!=null && editedData !=null){				MesquiteInteger pos = new MesquiteInteger(0);				int site= MesquiteInteger.fromFirstToken(arguments, pos);				int localSite= MesquiteInteger.fromString(arguments, pos);				String s = ParseUtil.getToken(arguments, pos);				int whichPanel= MesquiteInteger.fromString(arguments, pos);				int cx= MesquiteInteger.fromString(arguments, pos);				if (!MesquiteInteger.isCombinable(cx))					cx = 1;				ChromViewContext context = getContext(cx);				if (context == null){					MesquiteMessage.warnProgrammer("No context found for chrominit");					return null;				}				if ("false".equalsIgnoreCase(s) && whichPanel == 2){ //edited sequence					SequencePanel panel = window.getSequencePanel(whichPanel);					ContigDisplay contigDisplay = window.getContigDisplay();					ChromaseqUniversalMapper universalMapper = contigDisplay.getUniversalMapper();					ContigMapper contigMapper = universalMapper.getContigMapper();					int ic = panel.getCanvas().matrixBaseFromSequenceBase(localSite);					int it = context.taxon.getNumber();					MesquiteBoolean dataChanged = new MesquiteBoolean(false);					if (ic < 0)						return null;					int icMove = ic;					if (contigDisplay.isReversedInEditedData())  //shift by one as reversed						icMove++;					int contigBase = universalMapper.getOtherBaseFromEditedMatrixBase(ChromaseqUniversalMapper.ACEFILECONTIG, icMove);					int added = editedData.moveCells(icMove, editedData.lastApplicable(it), 1, it,it, true, false, true, false, dataChanged, null);					editedData.setState(icMove, it, CategoricalState.unassigned);					ChromaseqUtil.setIntegerCellObject(editedData,ChromaseqUtil.chromaseqCellFlagsNameRef, icMove, it, new MesquiteInteger(2));					int contigBaseToUndelete = -1;					int addToFirstTrimmedBase = 0;					int addToLastTrimmedBase = 0;					boolean atStart = false;					boolean atEnd = false;					if (contigBase<contigMapper.getNumTrimmedFromStart()){ // it's before the trimmed area						if (contigMapper.getNumLeftToResurrectAtStart()>0)							contigBaseToUndelete = contigMapper.getNumLeftToResurrectAtStart()-1;						else							addToFirstTrimmedBase=1;						atStart=true;					} else if (contigBase>contigMapper.getLastTrimmedContigBase()){						if (contigMapper.getNumLeftToResurrectAtEnd()>0)							contigBaseToUndelete = contigMapper.getNumBases()-contigMapper.getNumLeftToResurrectAtEnd();						else							addToLastTrimmedBase=1;						atEnd = true;					}					if (added!=0)  {						editedData.notifyInLinked(new Notification(CharacterData.PARTS_ADDED));						editedData.notifyListeners(this, new Notification(CharacterData.PARTS_ADDED));					}					else if (dataChanged.getValue()) {						Notification notification = new Notification(CharacterData.DATA_CHANGED, new int[] {it});						notification.setSubcodes(new int[] {MesquiteListener.SINGLE_TAXON});						editedData.notifyInLinked(notification);						editedData.notifyListeners(this, notification);					}					if (contigBaseToUndelete>=0){						MeristicData reverseRegistryData = ChromaseqUtil.getReverseRegistryData(registryData);						contigMapper.setDeletedBase(contigBaseToUndelete, false);						int editedBase=0;						if (atStart) {							for (int base = contigBaseToUndelete; base<contigBase; base++){								editedBase = universalMapper.getEditedMatrixBaseFromOtherBase(ChromaseqUniversalMapper.ACEFILECONTIG, base+1);								int icOriginal = registryData.getState(editedBase, it);								editedBase = universalMapper.getEditedMatrixBaseFromOtherBase(ChromaseqUniversalMapper.ACEFILECONTIG, base);								registryData.setState(editedBase, it, 0,icOriginal);							}							editedBase = universalMapper.getEditedMatrixBaseFromOtherBase(ChromaseqUniversalMapper.ACEFILECONTIG, contigBase);							registryData.setToInapplicable(editedBase, it);							//reverseRegistryData.setState(contigBaseToUndelete,it,0,ic);							contigMapper.setNumResurrectedAtStart(contigMapper.getNumResurrectedAtStart()+1);						} else if (atEnd) {							for (int base = contigBaseToUndelete; base>contigBase; base--){								editedBase = universalMapper.getEditedMatrixBaseFromOtherBase(ChromaseqUniversalMapper.ACEFILECONTIG, base-1);								int icOriginal = registryData.getState(editedBase, it);								editedBase = universalMapper.getEditedMatrixBaseFromOtherBase(ChromaseqUniversalMapper.ACEFILECONTIG, base);								registryData.setState(editedBase, it, 0,icOriginal);							}							registryData.setToInapplicable(editedBase, it);							contigMapper.setNumResurrectedAtEnd(contigMapper.getNumResurrectedAtEnd()+1);						}						//ChromaseqUtil.fillReverseRegistryData(reverseRegistryData);						//	registryData.setState(ic,it,0,contigBaseToUndelete);						//	reverseRegistryData.setState(contigBaseToUndelete,it,0,ic);					}					else						ChromaseqUtil.specifyBaseAsAdded(contigDisplay, editedData, icMove, it, -1,-1);					/*//						Debugg.println(" basesAdded: " + basesAdded);					int universalBaseOfStartContig = universalMapper.getUniversalBaseFromOtherBase(ChromaseqUniversalMapper.ACEFILECONTIG, 0);					int universalBaseOfEndContig = universalMapper.getUniversalBaseFromOtherBase(ChromaseqUniversalMapper.ACEFILECONTIG, contigPanel.getContig().getNumBases()-1);					for (int universalBase = 0; universalBase< universalMapper.getNumUniversalBases() && universalBase<addedBase.length; universalBase++){						if (addedBase[universalBase]){ 							int contigBase = contigPanel.getContigBaseFromUniversalBase(universalBase);							//										Debugg.println("*   universalBase: " + universalBase);							//										Debugg.println("    contigBase: " + contigBase);							if (contigBase>=0) {  // let's only mark the ones that match contigs								ChromaseqUtil.specifyBaseAsAdded(contigPanel, editedData, contigPanel.getMatrixPositionOfUniversalBase(universalBase, originalData)+selectOffset, it, contigBase, -1);							} else if (universalBase <= universalBaseOfStartContig){ 								ChromaseqUtil.specifyBaseAsAdded(contigPanel, editedData, contigPanel.getMatrixPositionOfUniversalBase(universalBase, originalData)+selectOffset, it, contigBase, 0);							} else if (universalBase >= universalBaseOfEndContig){ 								ChromaseqUtil.specifyBaseAsAdded(contigPanel, editedData, contigPanel.getMatrixPositionOfUniversalBase(universalBase, originalData)+selectOffset, it, contigBase, contigPanel.getContig().getNumBases()-1);							} else { 								ChromaseqUtil.specifyBaseAsAdded(contigPanel, editedData, contigPanel.getMatrixPositionOfUniversalBase(universalBase, originalData)+selectOffset, it, contigBase, -1);							}						}					}					contigPanel.getContigMapper().recalc(it);					 */								Notification notification = new Notification(CharacterData.DATA_CHANGED, new int[] {it});					notification.setSubcodes(new int[] {MesquiteListener.SINGLE_TAXON});					editedData.notifyListeners(this, notification);					panel.getCanvas();					//	((MatrixSequence)panel.getCanvas().getSequence()).calculateMappingToSourceData();					context.sequences[2].getCanvas().selectAndFocusLocalPosition(localSite);										window.setToPreviousTool();										window.repaintAll();					table.repaintAll();				}			}		}		else			return  super.doCommand(commandName, arguments, checker);		return null;	}	/*.................................................................................................................*/	public String getName() {		return "Insert Base";	}	/*.................................................................................................................*/	/** returns an explanation of what the module does.*/	public String getExplanation() {		return "Supplies tool to inserts bases in the edited sequence." ;	}}